import { metadata as charadesMetadata } from './metadata';

export const metadata = charadesMetadata;

export default function CharadesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
